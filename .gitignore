# Hugo
/public/          # Сгенерированный сайт (деплоится отдельно)
/resources/_gen/   # Кеш Hugo
.hugo_build.lock

# Темы (если не используете подмодуль)
/themes/*
!/themes/PaperMod  # Если тема в подмодуле, разрешаем только её

# Системные файлы
.DS_Store
*.swp
*.bak
